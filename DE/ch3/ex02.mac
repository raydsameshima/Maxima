/* ex02.mac */

kill(y,x)$
depends(y,x);
eqn: 9*y*diff(y,x)+4*x = 0;
sol: ode2(eqn,y,x);
sol: ic1(sol, x = 0,y = 1);
sol: radcan(expand(sol));


load(draw);
draw2d(implicit(sol,x,-2,2,y,-2,2));

kill(y,x)$
eqn: x*'diff(y,x)=2*x-y;
sol: ode2(eqn,y,x);
solve(%,y);
define(y(x), rhs(%[1]));
x*diff(y(x),x)-(2*x-y(x)), expand;
